Goal:  Get this to run against eutilstest, such that we see a list of real failures that
ought to be fixed.

STEPS

✓ fetch-dtd
✓ fetch-xml
    ✓ Get filter params working
    ✓ Are there any step-specific options?  No
    ✗ defer: Fix the summary report
        - Record every message, and a flag for failed,
          in every samplegroup and sample
✓ validate-xml
    - Change it so that the munged XML files is also in the out dir.
✓ generate-xslt
- generate-json
- validate-json

